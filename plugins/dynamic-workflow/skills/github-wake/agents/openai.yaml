interface:
  display_name: "GitHub Reply Wakeup"
  short_description: "Wake Codex or Claude when a GitHub issue gets a reply"
  default_prompt: "Use $github-wake to register an existing GitHub issue and wake this agent session when its first future reply arrives."
