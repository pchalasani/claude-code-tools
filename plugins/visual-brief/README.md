# Visual Brief plugin

This dual-provider plugin exposes the Visual Brief skill and adds quiet
milestone reminders after a successful `visual-brief publish`. Installing the
`visual-brief` CLI is a prerequisite for the command and reminders.

Codex users should review and trust the hook definition before enabling it.
