# Visual Brief reminder plugin

This dual-provider plugin adds quiet milestone reminders after a successful
`visual-brief publish`. Installing the `visual-brief` CLI is a prerequisite.
The plugin is inert when that command is unavailable.

Codex users should review and trust the hook definition before enabling it.
